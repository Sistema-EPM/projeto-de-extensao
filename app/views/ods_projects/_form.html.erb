<%= form_with(model: ods_project) do |form| %>
  <% if ods_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ods_project.errors.count, "error") %> prohibited this ods_project from being saved:</h2>

      <ul>
        <% ods_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
