<p style="color: green"><%= notice %></p>

<%= render @responsible %>

<div>
  <%= link_to "Editar", edit_responsible_path(@responsible) %> |
  <%= link_to "Voltar", responsibles_path %>

  <% if (admin_signed_in? || current_user.try(:is_responsible)) %>
    <%= button_to "Destroy this user", @responsible, method: :delete %>
  <% end %>
</div>
